#mysql
spring:
  datasource:
    #mysql≈‰÷√
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/te?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
mybatis:
  #  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.model.demo

server:
  port: 9000
